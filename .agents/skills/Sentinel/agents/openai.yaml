interface:
  display_name: "Sentinel"
  short_description: "Repo-specific patterns and workflows for Sentinel"
  default_prompt: "Use the Sentinel repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true